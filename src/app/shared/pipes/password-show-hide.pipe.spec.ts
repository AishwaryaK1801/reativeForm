import { PasswordShowHidePipe } from './password-show-hide.pipe';

describe('PasswordShowHidePipe', () => {
  it('create an instance', () => {
    const pipe = new PasswordShowHidePipe();
    expect(pipe).toBeTruthy();
  });
});
